{
  "name": "mesdoc-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "yarn --cwd mesdoc build",
    "start": "yarn --cwd mesdoc start",
    "deploy": "npx vercel --prod"
  }
}
